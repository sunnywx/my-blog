import {h} from 'preact'
import style from './index.scss'

type Props = {
  className?: string;
}

function Tags(props: Props) {
  return (
    <div>
      tags
    </div>
  );
}

export default Tags;